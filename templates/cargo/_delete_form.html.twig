<form method="post" action="{{ path('app_cargo_delete', {'id': cargo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cargo.id) }}">
    <button class="btn">Delete</button>
</form>
